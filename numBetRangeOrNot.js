/*9)Find the a number is present in given range Print “Between the range” if num is between start and end           	
    values Else Print “Outside the range” since num is outside start and end values.*/

const number=5;
const start=1;
const end =10;
if(number>=start && number<=end)
{
    console.log("number is Between the range");
}
else
{
    console.log("number is outside the range");
}